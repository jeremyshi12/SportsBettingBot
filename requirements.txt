# ── Core ────────────────────────────────────────────────────────────────
requests>=2.31.0
aiohttp>=3.9.0
websockets>=12.0
pyyaml>=6.0

# ── Data ────────────────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.25.0

# ── Machine learning ────────────────────────────────────────────────────
# scipy powers the exact normal/t quantiles behind PSR, DSR and the
# minimum-detectable-edge calculation. src/backtest/metrics.py falls back to
# a rational approximation without it, but install it for research runs.
scikit-learn>=1.3.0
scipy>=1.11.0
joblib>=1.3.0

# Gradient boosting. src/ml/backends.py prefers xgboost and falls back to
# sklearn's HistGradientBoosting, so neither of these is required.
xgboost>=2.0.0     # optional
lightgbm>=4.0.0    # optional

# ── Optional ────────────────────────────────────────────────────────────
# Smoother volatility-regime transitions; percentile method is used without it.
hmmlearn>=0.3.0    # optional

# Live sentiment features. NOT used in research runs -- a live news lookup
# while replaying historical markets is look-ahead (FeatureEngine defaults to
# allow_live_sentiment=False). Install only for live trading.
duckduckgo-search>=5.2.1   # optional, live only
textblob>=0.18.0           # optional, live only

# Visualisation
matplotlib>=3.7.0

# ── Testing ─────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
